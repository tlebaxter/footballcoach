package CFBsimPack.engine.snap;

/** Defensive assignment slot for rush / coverage / gap fits. */
public enum DefSlot {
    LE, DT, NT, RE,
    EDGE_L, EDGE_R,
    WILL, MIKE, SAM, LB,
    NB, CB_L, CB_R,
    FS, SS, S,
    SPY
}
