package CFBsimPack.engine.snap;

/** Offensive assignment slot for protection / routes / run tracks. */
public enum OffSlot {
    QB,
    LT, LG, C, RG, RT,
    TE_L, TE_R,
    RB, FB,
    WR_X, WR_Z, WR_SLOT, WR_H
}
